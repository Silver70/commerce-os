/** Feature-local UI types for the catalog. Shared API shapes live in types/api. */

/** A variant picker's current selection: option id → chosen option-value id. */
export type OptionSelection = Record<string, string>;
