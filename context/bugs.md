While tracing units I noticed the backend pricing engine likely has a 100× bug for percentage coupons/discounts on the storefront checkout path: pricing-engine.service.ts:347 calls applyPercentage(subtotal, coupon.value), and applyPercentage divides by 10000 (basis points) — but the value is stored as whole percent (20, not 2000). So a "20% off" coupon would only take 0.2% off at storefront checkout. This is a different flow from the admin order bug you reported, so I left it alone — want me to look into it separately?
